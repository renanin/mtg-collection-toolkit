.mtg-block(@click="togglePopup()")
  h2 {{name}}
  .popup(:class="{ active: popup }")
    h2 {{name}}
    .sets
      mtg-set(v-for="(set, key) in block",:key="key",:set="set")
  .popup-background(:class="{ active: popup }")
